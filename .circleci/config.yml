version: 2
jobs:
  build:
    branches:
      only:
        - master
    docker:
      - image: alpine
    steps:
      - add_ssh_keys:
          fingerprints:
            - "90:08:7b:d5:d6:75:ad:d5:97:9f:e0:29:73:f5:ad:1c"
      - checkout
      - run:
          name: gh-pages
          command: sh .circleci/publish.sh